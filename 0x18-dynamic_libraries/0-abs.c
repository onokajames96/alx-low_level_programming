#include "main.h"
/**
 *_abs - calculates the absolute value ofan integer
 *@n: value to be checked
 * Return: Absolute value.
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
